const template = document.getElementById('template');
let mapRegion = document.querySelectorAll('.map-region__region');
let tmpPeriod = document.querySelector('.line-period  span');
for (let index = 0; index < mapRegion.length; index++) {
	const element = mapRegion[index];
	let isHidden = true;
	element.addEventListener('mouseover', (e) => {
		
		console.log( );
		if(element.closest('.map-region__region')) {
			let tippu = tippy('.map-region__region', {
				content: `<div class="interactive__block" id="template">
				<div class="interactive__body">
				  <div class="interactive__header">
					<h4>${element.dataset.title}</h4>
				  </div>
				  <div class="interactive__row">
					<div class="interactive__line line-period">
					  <p>Период</p><span>${element.dataset.period}</span>
					</div>
					<div class="interactive__line line-subject">
					  <p>Субъекты ДПО</p> <span>${element.dataset.subject}</span>
					</div>
					<div class="interactive__line">
					  <p>Программы ДПО</p><span>${element.dataset.programs}</span>
					</div>
					<div class="interactive__line">
					  <p>Метод центры</p><span>${element.dataset.method}</span>
					</div>
					<div class="interactive__line">
					  <p>ДПО на базе ВО</p> <span>${element.dataset.dpo}</span>
					</div>
				  </div>
				  <div class="interactive__link">
					<a href="#">Подробная статистике по региону</a>
				  </div>
				</div>
			  </div>`,
				  allowHTML: true,
				  trigger: 'click'
			});
		} else {
			tippu.hide();
		}
		
		
	 })
	
	// document.querySelector(".interactive__header h4").innerHTML = element.title;
	// tmpPeriod.innerText = element.dataset.mapPeriod;
}